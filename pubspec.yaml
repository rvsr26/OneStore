name: ecommerce_app
description: Super App E-Commerce
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  
  # Core
  provider: ^6.1.2
  cupertino_icons: ^1.0.8
  cached_network_image: ^3.4.1
  intl: ^0.19.0
  shared_preferences: ^2.2.2
  
  # 🔥 Firebase (VERSION FIXED - ALIGNED TO LATEST)
  firebase_core: ^3.1.0
  firebase_auth: ^5.1.0
  cloud_firestore: ^5.0.1
  firebase_storage: ^12.0.0
  firebase_messaging: ^15.0.0  # Updated to match Core v3
  
  # Payments & Network
  razorpay_flutter: ^1.3.7
  http: ^1.2.1
  
  # Features
  image_picker: ^1.1.1
  share_plus: ^10.0.0       
  pdf: ^3.11.0              
  printing: ^5.13.0         
  scratcher: ^2.5.0         
  confetti: ^0.7.0          
  speech_to_text: ^7.0.0    
  permission_handler: ^11.3.1 
  local_auth: ^2.1.6          
  flutter_local_notifications: ^17.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true